You are working in my SPA (Vite/React + optional Express API). The "Add New Supplier" modal has a Save button that does nothing. Fix it end-to-end.

CONTEXT & EXPECTED FIELDS
- Modal title: "Add New Supplier"
- Inputs: supplierName* (text), contactPerson (text), phone (text), address (textarea),
  state* (select), city* (text)
- Buttons: Cancel, Save
- * = required

WHAT TO DO
FRONTEND (React)
1) Locate the modal/component (likely AddSupplierModal.(jsx|tsx) or SupplierForm.(jsx|tsx)).
2) Ensure the inputs are inside a `<form>` with an `onSubmit` handler. The Save button MUST be:
   - `type="submit"`
   - NOT permanently disabled
   - Shows loading state while saving
3) Implement (or fix) `handleSubmit`:
   - `event.preventDefault()`
   - Read current field values (controlled state or `FormData`)
   - Validate required fields: supplierName, state, city (trim & non-empty). If invalid, show inline errors and DO NOT call the API.
   - POST to the existing create endpoint (see BACKEND section); headers: `Content-Type: application/json`
   - On 2xx: close modal and refresh supplier list (call existing reload/mutate hook or refetch).
   - On non-2xx or network error: show a visible error message/toast; keep modal open.
4) If the current Save button uses an `onClick` without form submit: keep that but still wire `onSubmit` on the `<form>`; avoid double submissions.
5) Add `data-testid="save-button"` to the Save button for quick verification.
6) If there is any overlay that intercepts clicks, ensure button is not covered and is enabled.

SAMPLE PATCHES (apply if missing; adapt filenames)
- Form & button:
  ```diff
  - <div className="actions"><button className="btn btn-primary">Save</button></div>
  + <form onSubmit={handleSubmit} noValidate>
  +   {/* ...all inputs... each has name/id matching payload keys */}
  +   <div className="actions">
  +     <button type="button" className="btn" onClick={onCancel}>Cancel</button>
  +     <button type="submit" data-testid="save-button" className="btn btn-primary" disabled={isSaving}>
  +       {isSaving ? "Saving..." : "Save"}
  +     </button>
  +   </div>
  + </form>
